[html]
page_title = ${PAGE_TITLE}
[ldap]
host = ${LDAP_HOST}
port = ${LDAP_PORT}
use_ssl = ${USE_SSL}
base = ${LDAP_BASE}
type = ${DC_TYPE}
ad_domain = ${AD_DOMAIN}
search_filter = ${AD_SEARCH_FILTER}
[server]
server = ${SERVER}
host = ${SERVER_HOST}
port = ${SERVER_PORT}